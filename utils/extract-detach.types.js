'use strict';

/**
 * @file Type definitions for {@link module:utils/extract-detach}.
 */

/**
 * @typedef {object} DetachResult
 * @property {string[]} cmd — cleaned command array with detach syntax removed
 * @property {boolean} detach — whether detach intent was detected
 */

module.exports = {};
